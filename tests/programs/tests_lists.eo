(include "../../signature/theories/theory.eo")
(include "../../signature/programs/lists.eo")
(include "../unit_test_services.eo")

; Unit tests for lists.eo.

;---------------------------------
; $f_list_length
;---------------------------------

; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(declare-const d Int)
(declare-const e Int)
(declare-const f Int)

(step $test_$f_list_length_1 true 
      :rule $assert_eq 
      :args (($f_list_length and (and a))
             
             2))

(step $test_$f_list_length_2 true 
      :rule $assert_eq 
      :args (($f_list_length and (and a b))
             
             3))

(step $test_$f_list_length_3 true 
      :rule $assert_eq 
      :args (($f_list_length and (and a b c))
             
             4))

(step $test_$f_list_length_4 true 
      :rule $assert_eq 
      :args (($f_list_length and (and a b (and a b c)))
             
             4))

(step $test_$f_list_length_5 true 
      :rule $assert_eq 
      :args (($f_list_length or (or a))
             
             2))

(step $test_$f_list_length_6 true 
      :rule $assert_eq 
      :args (($f_list_length or (or a b))
             
             3))

(step $test_$f_list_length_7 true 
      :rule $assert_eq 
      :args (($f_list_length or (or a b c))
             
             4))

(step $test_$f_list_length_8 true 
      :rule $assert_eq 
      :args (($f_list_length or (or a b (or a b c)))
             
             4))

(step $test_$f_list_length_9 true 
      :rule $assert_eq 
      :args (($f_list_length or (or a (or a b c) b))
             
             4))

(step $test_$f_list_length_10 true 
      :rule $assert_eq 
      :args (($f_list_length + (+ d e))
             
             3))

(step $test_$f_list_length_11 true 
      :rule $assert_eq 
      :args (($f_list_length + (+ d d e))
             
             4))

(step $test_$f_list_length_12 true 
      :rule $assert_eq 
      :args (($f_list_length + (+ d d (* d d e)))
             
             4))

(step $test_$f_list_length_13 true 
      :rule $assert_eq 
      :args (($f_list_length + (+ d (* d d e) b))
             
             4))


;---------------------------------
; $f_list_equal_mod_commut
;---------------------------------

; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(declare-const d Int)
(declare-const e Int)
(declare-const f Int)

(step $test_$f_list_equal_mod_commut_1 true 
      :rule $assert_eq 
      :args (($f_list_equal_mod_commut and (and a b) (and b a))
             
             true))

(step $test_$f_list_equal_mod_commut_2 true 
      :rule $assert_eq 
      :args (($f_list_equal_mod_commut and (and a b c) (and b c a))
             
             true))

(step $test_$f_list_equal_mod_commut_3 true 
      :rule $assert_eq 
      :args (($f_list_equal_mod_commut and (and a (and a b c) c) (and (and a b c) c a))
             
             true))

(step $test_$f_list_equal_mod_commut_4 true 
      :rule $assert_eq 
      :args (($f_list_equal_mod_commut and (and a (and b a c) c) (and (and a b c) c a))
             
             false))

(step $test_$f_list_equal_mod_commut_5 true 
      :rule $assert_eq 
      :args (($f_list_equal_mod_commut or (or a b) (or b a))
             
             true))

(step $test_$f_list_equal_mod_commut_6 true 
      :rule $assert_eq 
      :args (($f_list_equal_mod_commut or (or a b c) (or b c a))
             
             true))

(step $test_$f_list_equal_mod_commut_7 true 
      :rule $assert_eq 
      :args (($f_list_equal_mod_commut or (or a (or a b c) c) (or (or a b c) c a))
             
             true))

(step $test_$f_list_equal_mod_commut_8 true 
      :rule $assert_eq 
      :args (($f_list_equal_mod_commut or (or a (or b a c) c) (or (or a b c) c a))
             
             false))

;---------------------------------
; $f_list_equal_mod_commut_and_length
;---------------------------------

; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(declare-const d Int)
(declare-const e Int)
(declare-const f Int)

(step $test_$f_list_equal_mod_commut_and_length_1 true 
      :rule $assert_eq 
      :args (($f_list_equal_mod_commut_and_length or 
                                                  (or a false b) 
                                                  (or a b false))
             
             true))

;-------------
; $f_list_zip
;-------------

(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(declare-const d Bool)
(declare-const e Bool)
(declare-const f Bool)
(declare-const g Bool)
(declare-const h Bool)

(step $test_$f_list_zip_1 true 
      :rule $assert_eq 
      :args (($f_list_zip or false (or a b) (or c d) and)
             
             (or ($f_list_cons and a c) ($f_list_cons and b d))))

(step $test_$f_list_zip_2 true 
      :rule $assert_eq 
      :args (($f_list_zip or false (or a b c) (or d e f) and)
             
             (or ($f_list_cons and a d) 
                 ($f_list_cons and b e)
                 ($f_list_cons and c f))))

(step $test_$f_list_zip_3 true 
      :rule $assert_eq 
      :args (($f_list_zip or 
                          false 
                          (or (and a b) 
                              (and c d)) 
                          (or (and e f) 
                              (and g h))
                          and)
             
             (or ($f_list_cons and (and a b) (and e f)) 
                 ($f_list_cons and (and c d) (and g h)))))

(step $test_$f_list_zip_4 true 
      :rule $assert_eq 
      :args (($f_list_zip or 
                          false 
                          false
                          false
                          and)
             
             false))

;----------------
; $f_list_map_zip
;----------------

(define x1 () (eo::var "x1" Bool))
(define x2 () (eo::var "x2" Bool))
(define x3 () (eo::var "x3" Bool))

(step $test_$f_list_map_zip_1 true 
      :rule $assert_eq 
      :args (($f_list_map_zip @varlist 
                              @varlist.nil 
                              (@varlist (@varlist x1))
                              (@varlist (@varlist false))
                              @pair)
             
             (@varlist (@varlist (@pair x1 false)))))

(step $test_$f_list_map_zip_2 true 
      :rule $assert_eq 
      :args (($f_list_map_zip @varlist 
                              @varlist.nil 
                              (@varlist (@varlist x1)
                                        (@varlist x1))
                              (@varlist (@varlist false)
                                        (@varlist true))
                              @pair)
             
             (@varlist (@varlist (@pair x1 false))
                       (@varlist (@pair x1 true)))))

(step $test_$f_list_map_zip_3 true 
      :rule $assert_eq 
      :args (($f_list_map_zip @varlist 
                              @varlist.nil 
                              (@varlist (@varlist x1 x2) 
                                        (@varlist x1 x2)
                                        (@varlist x1 x2)
                                        (@varlist x1 x2))
                              (@varlist (@varlist false false)
                                        (@varlist true false)
                                        (@varlist false true)
                                        (@varlist true true))
                              @pair)
             
             (@varlist (@varlist (@pair x1 false)
                                 (@pair x2 false))
                       (@varlist (@pair x1 true)
                                 (@pair x2 false))
                       (@varlist (@pair x1 false)
                                 (@pair x2 true))
                       (@varlist (@pair x1 true)
                                 (@pair x2 true)))))

(step $test_$f_list_map_zip_4 true 
      :rule $assert_eq 
      :args (($f_list_map_zip @varlist 
                              @varlist.nil 
                              (@varlist (@varlist x1 x2 x3)
                                        (@varlist x1 x2 x3)
                                        (@varlist x1 x2 x3)
                                        (@varlist x1 x2 x3)
                                        (@varlist x1 x2 x3)
                                        (@varlist x1 x2 x3)
                                        (@varlist x1 x2 x3)
                                        (@varlist x1 x2 x3))
                              (@varlist (@varlist false false false)
                                        (@varlist true false false)
                                        (@varlist false true false)
                                        (@varlist true true false)
                                        (@varlist false false true)
                                        (@varlist true false true)
                                        (@varlist false true true)
                                        (@varlist true true true))
                              @varlist)
             
             (@varlist (@varlist ($f_list_cons @varlist x1 false)
                                 ($f_list_cons @varlist x2 false)
                                 ($f_list_cons @varlist x3 false))
                       (@varlist ($f_list_cons @varlist x1 true)
                                 ($f_list_cons @varlist x2 false)
                                 ($f_list_cons @varlist x3 false))
                       (@varlist ($f_list_cons @varlist x1 false)
                                 ($f_list_cons @varlist x2 true)
                                 ($f_list_cons @varlist x3 false))
                       (@varlist ($f_list_cons @varlist x1 true)
                                 ($f_list_cons @varlist x2 true)
                                 ($f_list_cons @varlist x3 false))
                       (@varlist ($f_list_cons @varlist x1 false)
                                 ($f_list_cons @varlist x2 false)
                                 ($f_list_cons @varlist x3 true))
                       (@varlist ($f_list_cons @varlist x1 true)
                                 ($f_list_cons @varlist x2 false)
                                 ($f_list_cons @varlist x3 true))
                       (@varlist ($f_list_cons @varlist x1 false)
                                 ($f_list_cons @varlist x2 true)
                                 ($f_list_cons @varlist x3 true))
                       (@varlist ($f_list_cons @varlist x1 true)
                                 ($f_list_cons @varlist x2 true)
                                 ($f_list_cons @varlist x3 true)))))
;--------------------
; $f_list_repeat_elem
;--------------------

(declare-const a Bool)

(step $test_$f_list_repeat_elem_1 true 
      :rule $assert_eq 
      :args (($f_list_repeat_elem or a false 0)

             false))

(step $test_$f_list_repeat_elem_2 true 
      :rule $assert_eq 
      :args (($f_list_repeat_elem or a false 1)

             (or a)))

(step $test_$f_list_repeat_elem_3 true 
      :rule $assert_eq 
      :args (($f_list_repeat_elem or a false 2)

             (or a a)))

(step $test_$f_list_repeat_elem_4 true 
      :rule $assert_eq 
      :args (($f_list_repeat_elem or a false 3)

             (or a a a)))

(step $test_$f_list_repeat_elem_5 true 
      :rule $assert_eq 
      :args (($f_list_repeat_elem or a false 4)

             (or a a a a)))

;--------------------------
; $f_list_remove_every_elem
;--------------------------

; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(declare-const d Int)
(declare-const e Int)
(declare-const f Int)

(step $test_$f_list_remove_every_elem_1 true 
      :rule $assert_eq 
      :args (($f_list_remove_every_elem and (and a true) true true)
             
             (and a)))

(step $test_$f_list_remove_every_elem_1b true 
      :rule $assert_eq 
      :args (($f_list_remove_every_elem and (and a b true) true true)
             
             (and a b)))

(step $test_$f_list_remove_every_elem_1c true 
      :rule $assert_eq 
      :args (($f_list_remove_every_elem and (and a b true c) true true)
             
             (and a b c)))

(step $test_$f_list_remove_every_elem_2 true 
      :rule $assert_eq 
      :args (($f_list_remove_every_elem and (and a true b true c) true true)
             
             (and a b c)))

(step $test_$f_list_remove_every_elem_3 true 
      :rule $assert_eq 
      :args (($f_list_remove_every_elem and (and true a true b true c) true true)
             
             (and a b c)))

(step $test_$f_list_remove_every_elem_4 true 
      :rule $assert_eq 
      :args (($f_list_remove_every_elem and (and true a a true b b true c) true true)
             
             (and a a b b c)))

;--------------------------
; $f_list_remove_duplicates
;--------------------------

; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(declare-const d Int)
(declare-const e Int)
(declare-const f Int)

(step $test_$f_list_remove_duplicates_1 true 
      :rule $assert_eq 
      :args (($f_list_remove_duplicates and (and a a))
             
             (and a)))

(step $test_$f_list_remove_duplicates_1b true 
      :rule $assert_eq 
      :args (($f_list_remove_duplicates and ($f_list_cons and a a))
             
             a))

(step $test_$f_list_remove_duplicates_1c true 
      :rule $assert_eq 
      :args (($f_list_remove_duplicates or ($f_list_cons or a a))
             
             a))

(step $test_$f_list_remove_duplicates_1d true 
      :rule $assert_eq 
      :args (($f_list_remove_duplicates and a)
             
             a))

(step $test_$f_list_remove_duplicates_2 true 
      :rule $assert_eq 
      :args (($f_list_remove_duplicates and (and a ))
             
             (and a)))

(step $test_$f_list_remove_duplicates_3 true 
      :rule $assert_eq 
      :args (($f_list_remove_duplicates and (and a b true c))
             
             (and a b c)))

(step $test_$f_list_remove_duplicates_4 true 
      :rule $assert_eq 
      :args (($f_list_remove_duplicates and (and true a true b true c))
             
             (and a b c)))

(step $test_$f_list_remove_duplicates_5 true 
      :rule $assert_eq 
      :args (($f_list_remove_duplicates and (and b true a a true b true c))
             
             (and a b c)))

(step $test_$f_list_remove_duplicates_6 true 
      :rule $assert_eq 
      :args (($f_list_remove_duplicates and (and c b true a a true b true c))
             
             (and a b c)))

;--------------------------
; $to_nil_terminated_f_list
;--------------------------

(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

(step $test_$to_nil_terminated_f_list_1 true 
      :rule $assert_eq 
      :args (($to_nil_terminated_f_list and true a)
             
             (and a)))

(step $test_$to_nil_terminated_f_list_2 true 
      :rule $assert_eq 
      :args (($to_nil_terminated_f_list and true ($f_list_cons and a b))
             
             (and a b)))

(step $test_$to_nil_terminated_f_list_2 true 
      :rule $assert_eq 
      :args (($to_nil_terminated_f_list and true ($f_list_cons and 
                                                               a 
                                                               ($f_list_cons and b c)))
             
             (and a b c)))

(step $test_$to_nil_terminated_f_list_3 true 
      :rule $assert_eq 
      :args (($to_nil_terminated_f_list and true (and a ($f_list_cons and b c)))
             
             (and a (and b c))))

(step $test_$to_nil_terminated_f_list_4 true 
      :rule $assert_eq 
      :args (($to_nil_terminated_f_list and true (and a ($f_list_cons or b c)))
             
             (and a ($f_list_cons or b c))))

(step $test_$to_nil_terminated_f_list_5 true 
      :rule $assert_eq 
      :args (($to_nil_terminated_f_list and true (and ($f_list_cons or a b) 
                                                      ($f_list_cons or b c)))
             
             (and ($f_list_cons or a b) ($f_list_cons or b c))))

(step $test_$to_nil_terminated_f_list_6 true 
      :rule $assert_eq 
      :args (($to_nil_terminated_f_list and true (and ($f_list_cons and a b)
                                                      ($f_list_cons and b c)))
             
             (and (and a b) (and b c))))

(step $test_$to_nil_terminated_f_list_7 true 
      :rule $assert_eq 
      :args (($to_nil_terminated_f_list and true (and ($f_list_cons and a b)
                                                      (and b c) c))
             
             (and (and a b) (and b c) c)))

; It is the identity function for already nil-terminated f-lists.
(step $test_$to_nil_terminated_f_list_8 true 
      :rule $assert_eq 
      :args (($to_nil_terminated_f_list and true (and a b))
             
             (and a b)))

(step $test_$to_nil_terminated_f_list_9 true 
      :rule $assert_eq 
      :args (($to_nil_terminated_f_list and true (and a b c))
             
             (and a b c)))

(step $test_$to_nil_terminated_f_list_10 true 
      :rule $assert_eq 
      :args (($to_nil_terminated_f_list and true (and a b c a))
             
             (and a b c a)))

(step $test_$to_nil_terminated_f_list_11 true 
      :rule $assert_eq 
      :args (($to_nil_terminated_f_list and true (and a b c (and a c)))
             
             (and a b c (and a c))))

