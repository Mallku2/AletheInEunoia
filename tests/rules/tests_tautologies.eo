(include "../../signature/rules/tautologies.eo")
(include "../unit_test_services.eo")


; Unit tests for rules that introduce tautologies and their helper programs.

;------------------------------------------
; $check_eq_transitive_remaining_equalities
;------------------------------------------
; Some constants to build terms, for testing purposes.
(declare-const U Type)
(declare-const a U)
(declare-const b U)
(declare-const c U)
(declare-const d U)
(declare-const e U)

(step $test_$check_eq_transitive_remaining_equalities_1 true
      :rule $assert_eq 
      :args (($check_eq_transitive_remaining_equalities
              a d (@cl (not (= a b)) (not (= b d))))

             true))

(step $test_$check_eq_transitive_remaining_equalities_2 true
      :rule $assert_eq 
      :args (($check_eq_transitive_remaining_equalities
              a d (@cl (not (= a b)) (not (= b c)) (not (= c d))))

             true))

(step $test_$check_eq_transitive_remaining_equalities_3 true
      :rule $assert_eq 
      :args (($check_eq_transitive_remaining_equalities
              a d (@cl (not (= a b)) (not (= b c)) (not (= c d))))

             true))

(step $test_$check_eq_transitive_remaining_equalities_4 true
      :rule $assert_eq 
      :args (($check_eq_transitive_remaining_equalities
              a e (@cl (not (= a b)) (not (= b c)) (not (= c d)) (not (= d e))))

             true))

;------------------------------------------
; $check_eq_transitive_from_first_equality
;------------------------------------------
; Some constants to build terms, for testing purposes.
(declare-const U Type)
(declare-const a U)
(declare-const b U)
(declare-const c U)
(declare-const d U)
(declare-const e U)

(step $test_$check_eq_transitive_from_first_equality_1 true
      :rule $assert_eq 
      :args (($check_eq_transitive_from_first_equality
              (= a d) (@cl (not (= a b)) (not (= b d))))

             true))

(step $test_$check_eq_transitive_from_first_equality_2 true
      :rule $assert_eq 
      :args (($check_eq_transitive_from_first_equality
              (= a d) (@cl (not (= a b)) (not (= b c)) (not (= c d))))

             true))

(step $test_$check_eq_transitive_from_first_equality_3 true
      :rule $assert_eq 
      :args (($check_eq_transitive_from_first_equality
              (= a d) (@cl (not (= a b)) (not (= b c)) (not (= c d))))

             true))

(step $test_$check_eq_transitive_from_first_equality_4 true
      :rule $assert_eq 
      :args (($check_eq_transitive_from_first_equality
              (= a e) (@cl (not (= a b)) (not (= b c)) (not (= c d)) (not (= d e))))

             true))

;------------------------------------------
; $check_eq_transitive
;------------------------------------------
; Some constants to build terms, for testing purposes.
(declare-const U Type)
(declare-const a U)
(declare-const b U)
(declare-const c U)
(declare-const d U)
(declare-const e U)

(step $test_$check_eq_transitive_1 true
      :rule $assert_eq 
      :args (($check_eq_transitive
              (@cl (not (= a b)) (not (= b d)) (= a d)))

             true))

(step $test_$check_eq_transitive_2 true
      :rule $assert_eq 
      :args (($check_eq_transitive
              (@cl (not (= a b)) (not (= b c)) (not (= c d)) (= a d)))

             true))

(step $test_$check_eq_transitive_3 true
      :rule $assert_eq 
      :args (($check_eq_transitive
              (@cl (not (= a b)) (not (= b c)) (not (= c d)) (= a d)))

             true))

(step $test_$check_eq_transitive_4 true
      :rule $assert_eq 
      :args (($check_eq_transitive
              (@cl (not (= a b)) (not (= b c)) (not (= c d)) (not (= d e))
                   (= a e) ))

             true))

;------------------------------------------
; eq_transitive
;------------------------------------------
; Some constants to build terms, for testing purposes.
(declare-const U Type)
(declare-const a U)
(declare-const b U)
(declare-const c U)
(declare-const d U)
(declare-const e U)

(step $test_eq_transitive_1 
      (@cl (not (= a b)) (not (= b d)) (= a d))
      :rule eq_transitive)

(step $test_eq_transitive_2 
      (@cl (not (= a b)) (not (= b c)) (not (= c d)) (= a d))
      :rule eq_transitive)

(step $test_eq_transitive_3
      (@cl (not (= a b)) (not (= b c)) (not (= c d)) (= a d))
      :rule eq_transitive)

(step $test_eq_transitive_4
      (@cl (not (= a b)) (not (= b c)) (not (= c d)) (not (= d e))
                   (= a e))
      :rule eq_transitive)



;--------------------------------------
; $check_n_ary_application_eq_congruent
;--------------------------------------
; Some constants to build terms, for testing purposes.
(declare-const U Type)
(declare-const a U)
(declare-const b U)
(declare-const c U)
(declare-const d U)
(declare-const e U)
(declare-const j U)
(declare-const g (-> U U))
(declare-const f (-> U U U))
(declare-const h (-> U U U U))

(step $test_$check_n_ary_application_eq_congruent_1 true 
      :rule $assert_eq 
      :args (($check_n_ary_application_eq_congruent (= (g a) (g b))
                                                    (@cl (not (= a b)))) 
             true))

(step $test_$check_n_ary_application_eq_congruent_2 true 
      :rule $assert_eq 
      :args (($check_n_ary_application_eq_congruent (= (f b c) (f d e))
                                                    (@cl (not (= c e))
                                                         (not (= b d)))) 
             true))

(step $test_$check_n_ary_application_eq_congruent_3 true 
      :rule $assert_eq 
      :args (($check_n_ary_application_eq_congruent (= (h a b c) (h d e j))
              (@cl (not (= c j)) (not (= b e)) (not (= a d)))) 
             true))

;--------------------
; $check_eq_congruent
;--------------------
; Some constants to build terms, for testing purposes.
(declare-const U Type)
(declare-const a U)
(declare-const b U)
(declare-const c U)
(declare-const d U)
(declare-const e U)
(declare-const j U)
(declare-const g (-> U U))
(declare-const f (-> U U U))
(declare-const h (-> U U U U))

(step $test_$check_eq_congruent_1 true 
      :rule $assert_eq 
      :args (($check_eq_congruent 
              (@cl (not (= a b))  (= (g a) (g b)))) true))

(step $test_$check_eq_congruent_2 true 
      :rule $assert_eq 
      :args (($check_eq_congruent (@cl (not (= b d)) (not (= c e)) (= (f b c) (f d e)))) 
             true))

(step $test_$check_eq_congruent_3 true 
      :rule $assert_eq 
      :args (($check_eq_congruent (@cl (not (= a d)) (not (= b e)) (not (= c j)) 
                                       (= (h a b c) (h d e j)))) 
             true))

;--------------------
; $check_eq_congruent_pred
;--------------------
; Some constants to build terms, for testing purposes.
(declare-const U Type)
(declare-const a U)
(declare-const b U)
(declare-const c U)
(declare-const d U)
(declare-const e U)
(declare-const j U)
(declare-const g (-> U U))
(declare-const f (-> U U Bool))
(declare-const h (-> U U U Bool))

(step $test_$check_eq_congruent_pred_1 true 
      :rule $assert_eq 
      :args (($check_eq_congruent_pred 
              (@cl (not (= a b))  (= (g a) (g b)))) true))

(step $test_$check_eq_congruent_pred_2 true 
      :rule $assert_eq 
      :args (($check_eq_congruent_pred (@cl (not (= b d)) (not (= c e)) 
                                            (= (f b c) (f d e)))) 
             true))

(step $test_$check_eq_congruent_pred_3 true 
      :rule $assert_eq 
      :args (($check_eq_congruent_pred (@cl (not (= a d)) (not (= b e)) (not (= c j)) 
                                       (= (h a b c) (h d e j)))) 
             true))

;---------
; xor_pos1
;---------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)

(step $test_xor_pos1_1
      (@cl (not (xor a b)) a b)
      :rule xor_pos1)

(step $test_xor_pos1_2
      (@cl (not (xor (or a true) b)) (or a true) b)
      :rule xor_pos1)

(step $test_xor_pos1_3
      (@cl (not (xor (or a true) (and b false))) (or a true) (and b false))
      :rule xor_pos1)

(step $test_xor_pos1_4
      (@cl (not (xor (xor a true) (and b false))) (xor a true) (and b false))
      :rule xor_pos1)

;---------
; xor_pos2
;---------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)

(step $test_xor_pos2_1
      (@cl (not (xor a b)) (not a) (not b))
      :rule xor_pos2)

(step $test_xor_pos2_2
      (@cl (not (xor (or a true) b)) (not (or a true)) (not b))
      :rule xor_pos2)

(step $test_xor_pos2_3
      (@cl (not (xor (or a true) (and b false))) (not (or a true)) (not (and b false)))
      :rule xor_pos2)

(step $test_xor_pos2_4
      (@cl (not (xor (xor a true) (and b false))) (not (xor a true)) (not (and b false)))
      :rule xor_pos2)

;---------
; xor_neg1
;---------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)

(step $test_xor_neg1_1
      (@cl (xor a b) a (not b))
      :rule xor_neg1)

(step $test_xor_neg1_2
      (@cl (xor (or a true) b) (or a true) (not b))
      :rule xor_neg1)

(step $test_xor_neg1_3
      (@cl (xor (or a true) (and b false)) (or a true) (not (and b false)))
      :rule xor_neg1)

(step $test_xor_neg1_4
      (@cl (xor (xor a true) (and b false)) (xor a true) (not (and b false)))
      :rule xor_neg1)

;---------
; xor_neg2
;---------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)

(step $test_xor_neg2_1
      (@cl (xor a b) (not a) b)
      :rule xor_neg2)

(step $test_xor_neg2_2
      (@cl (xor (or a true) b) (not (or a true)) b)
      :rule xor_neg2)

(step $test_xor_neg2_3
      (@cl (xor (or a true) (and b false)) (not (or a true)) (and b false))
      :rule xor_neg2)

(step $test_xor_neg2_4
      (@cl (xor (xor a true) (and b false)) (not (xor a true)) (and b false))
      :rule xor_neg2)

;---------
; equiv_neg1
;---------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)

(step $test_equiv_neg1_1
      (@cl (= a b) (not a) (not b))
      :rule equiv_neg1)

(step $test_equiv_neg1_2
      (@cl (= (or a true) b) (not (or a true)) (not b))
      :rule equiv_neg1)

(step $test_equiv_neg1_3
      (@cl (= (or a true) (and b false)) (not (or a true)) (not (and b false)))
      :rule equiv_neg1)

(step $test_equiv_neg1_4
      (@cl (= (xor a true) (and b false)) (not (xor a true)) (not (and b false)))
      :rule equiv_neg1)

;------------
; equiv_neg2
;------------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)

(step $test_equiv_neg2_1
      (@cl (= a b) a b)
      :rule equiv_neg2)

(step $test_equiv_neg2_2
      (@cl (= (or a true) b) (or a true) b)
      :rule equiv_neg2)

(step $test_equiv_neg2_3
      (@cl (= (or a true) (and b false)) (or a true) (and b false))
      :rule equiv_neg2)

(step $test_equiv_neg2_4
      (@cl (= (xor a true) (and b false)) (xor a true) (and b false))
      :rule equiv_neg2)


;---------
; ite_pos1
;---------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

(step $test_ite_pos1_1
      (@cl (not (ite a c b)) a b)
      :rule ite_pos1)

(step $test_ite_pos1_2
      (@cl (not (ite (or a true) c b)) (or a true) b)
      :rule ite_pos1)

(step $test_ite_pos1_3
      (@cl (not (ite (or a true) c (and b false))) (or a true) (and b false))
      :rule ite_pos1)

(step $test_ite_pos1_4
      (@cl (not (ite (xor a true) c (and b false))) (xor a true) (and b false))
      :rule ite_pos1)

;---------
; ite_pos2
;---------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

(step $test_ite_pos2_1
      (@cl (not (ite a b c)) (not a) b)
      :rule ite_pos2)

(step $test_ite_pos2_2
      (@cl (not (ite (or a true) b c)) (not (or a true)) b)
      :rule ite_pos2)

(step $test_ite_pos2_3
      (@cl (not (ite (or a true) (and b false) c)) 
           (not (or a true)) 
           (and b false))
      :rule ite_pos2)

(step $test_ite_pos2_4
      (@cl (not (ite (xor a true) (and b false) c)) 
           (not (xor a true)) 
           (and b false))
      :rule ite_pos2)

;---------
; ite_neg1
;---------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

(step $test_ite_neg1_1
      (@cl (ite a c b) a (not b))
      :rule ite_neg1)

(step $test_ite_neg1_2
      (@cl (ite (or a true) c b) (or a true) (not b))
      :rule ite_neg1)

(step $test_ite_neg1_3
      (@cl (ite (or a true) c (and b false)) (or a true) (not (and b false)))
      :rule ite_neg1)

(step $test_ite_neg1_4
      (@cl (ite (xor a true) c (and b false)) (xor a true) (not (and b false)))
      :rule ite_neg1)

;---------
; ite_neg2
;---------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

(step $test_ite_neg2_1
      (@cl (ite a b c) (not a) (not b))
      :rule ite_neg2)

(step $test_ite_neg2_2
      (@cl (ite (or a true) b c) (not (or a true)) (not b))
      :rule ite_neg2)

(step $test_ite_neg2_3
      (@cl (ite (or a true) (and b false) c) (not (or a true)) (not (and b false)))
      :rule ite_neg2)

(step $test_ite_neg2_4
      (@cl (ite (xor a true) (and b false) c) (not (xor a true)) (not (and b false)))
      :rule ite_neg2)

;--------------------
; $connective_def
;--------------------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

; First case
(step $test_$connective_def_1 true 
      :rule $assert_eq 
      :args (($connective_def (xor a b)) 
             
             ($f_list_cons or 
                  ($f_list_cons and (not a) b)
                  ($f_list_cons and a (not b)))))

; Second case
(step $test_$connective_def_2 true 
      :rule $assert_eq 
      :args (($connective_def (= a b)) 
             
             ($f_list_cons and (=> a b) (=> b a))))

; Third case
(step $test_$connective_def_3 true 
      :rule $assert_eq 
      :args (($connective_def (ite a b c)) 
             
             ($f_list_cons and (=> a b) (=> (not a) c))))

; Fourth case
(step $test_$connective_def_4 true 
      :rule $assert_eq 
      :args (($connective_def (forall @varlist.nil a)) 
             
             (not (exists @varlist.nil (not a)))))

; Fifth case
(step $test_$connective_def_4 true 
      :rule $assert_eq 
      :args (($connective_def (exists @varlist.nil (not a))) 
             
             (not (forall @varlist.nil (not (not a))))))

;--------------------
; $check_connective_def
;--------------------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

; First case
(step $test_$check_connective_def_1 true 
      :rule $assert_eq 
      :args (($check_connective_def 
              (@cl (= (xor a b)

                      ($f_list_cons or 
                                    ($f_list_cons and (not a) b)
                                    ($f_list_cons and a (not b))))))

             true))

; Second case
(step $test_$check_connective_def_2 true 
      :rule $assert_eq 
      :args (($check_connective_def (@cl (= (= a b)
                                            ($f_list_cons and (=> a b) (=> b a)))))
             
             true))

; Third case
(step $test_$check_connective_def_3 true 
      :rule $assert_eq 
      :args (($check_connective_def (@cl (= (ite a b c)
                                            ($f_list_cons and (=> a b) (=> (not a) c)))))

             true))

; Fourth case
(step $test_$check_connective_def_4 true 
      :rule $assert_eq 
      :args (($check_connective_def (@cl (= (forall @varlist.nil a)
                                            (not (exists @varlist.nil (not a))))))
             
             true))

; Fifth case
(step $test_$check_connective_def_4 true 
      :rule $assert_eq 
      :args (($check_connective_def (@cl (= (exists @varlist.nil (not a))
                                            (not (forall @varlist.nil (not (not a)))))))

             true))

;-------------
; $and_simplify_rec
;-------------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

; First case of $and_simplify_rec
(step $test_$and_simplify_rec_1 true 
      :rule $assert_eq 
      :args (($and_simplify_rec (and true true)) true))


; Second case of $and_simplify_rec
(step $test_$and_simplify_rec_2 true 
      :rule $assert_eq 
      :args (($and_simplify_rec (and true a b true)) 
             ($f_list_cons and a b)))

(step $test_$and_simplify_rec_3 true 
      :rule $assert_eq 
      :args (($and_simplify_rec (and true a b true c true)) 
             ($f_list_cons and a ($f_list_cons and b c))))


; Fourth case of $and_simplify_rec
(step $test_$and_simplify_rec_4 true 
      :rule $assert_eq 
      :args (($and_simplify_rec (and a false true)) 
             false))


;-------------
; and_simplify
;-------------

; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

; First case of $and_simplify_rec
(step $test_$and_simplify_1
      (@cl (= true true))
      :rule and_simplify)

(step $test_$and_simplify_2
      (@cl (= (and true true) true))
      :rule and_simplify)

(step $test_$and_simplify_3
      (@cl (= (and true true true) true))
      :rule and_simplify)

(step $test_$and_simplify_4
      (@cl (= (and true true true true) true))
      :rule and_simplify)

; Second case of $and_simplify_rec
(step $test_$and_simplify_5
      (@cl (= (and a true) a))
      :rule and_simplify)

(step $test_$and_simplify_6
      (@cl (= (and true a) a))
      :rule and_simplify)

(step $test_$and_simplify_7
      (@cl (= (and true a true) a))
      :rule and_simplify)

(step $test_$and_simplify_8
      (@cl (= (and true true a true) a))
      :rule and_simplify)

(step $test_$and_simplify_9
      (@cl (= (and true true true a) a))
      :rule and_simplify)

(step $test_$and_simplify_10
      (@cl (= (and true a true b) 
              ($f_list_cons and a b)))
      :rule and_simplify)

(step $test_$and_simplify_11
      (@cl (= (and true a b c true) 
              ($f_list_cons and a ($f_list_cons and b c))))
      :rule and_simplify)

; Third case of $and_simplify_rec
(step $test_$and_simplify_12
      (@cl (= (and a a true) a))
      :rule and_simplify)

(step $test_$and_simplify_13
      (@cl (= (and a true a) a))
      :rule and_simplify)

(step $test_$and_simplify_14
      (@cl (= (and a true a true) a))
      :rule and_simplify)

(step $test_$and_simplify_15
      (@cl (= (and true a true a true) a))
      :rule and_simplify)

(step $test_$and_simplify_16
      (@cl (= (and true a a a) a))
      :rule and_simplify)

(step $test_$and_simplify_17
      (@cl (= (and true a b true a b) 
              ($f_list_cons and a b)))
      :rule and_simplify)

(step $test_$and_simplify_18
      (@cl (= (and true a b c c true a) 
              ($f_list_cons and b ($f_list_cons and c a))))
      :rule and_simplify)

; Fourth case of $and_simplify_rec
(step $test_$and_simplify_19
      (@cl (= (and a false) false))
      :rule and_simplify)

(step $test_$and_simplify_20
      (@cl (= (and a false a) false))
      :rule and_simplify)

(step $test_$and_simplify_21
      (@cl (= (and a false a true) false))
      :rule and_simplify)

(step $test_$and_simplify_22
      (@cl (= (and false a true a true) false))
      :rule and_simplify)

(step $test_$and_simplify_23
      (@cl (= (and false a a true) false))
      :rule and_simplify)

(step $test_$and_simplify_24
      (@cl (= (and false a b false true b) 
              false))
      :rule and_simplify)

(step $test_$and_simplify_25
      (@cl (= (and false a b c c true a) 
              false))
      :rule and_simplify)

; Fifth case of $and_simplify_rec
(step $test_$and_simplify_26
      (@cl (= (and (not a) a) 
              false))
      :rule and_simplify)

(step $test_$and_simplify_27
      (@cl (= (and a (not a)) 
              false))
      :rule and_simplify)

(step $test_$and_simplify_28
      (@cl (= (and a (not (not (not a))))
              false))
      :rule and_simplify)

(step $test_$and_simplify_29
      (@cl (= (and (not (not a)) (not (not (not a))))
              false))
      :rule and_simplify)

(step $test_$and_simplify_30
      (@cl (= (and (not (not a)) true (not (not (not a))))
              false))
      :rule and_simplify)

(step $test_$and_simplify_31
      (@cl (= (and (not (not (not (not a)))) true (not (not (not a))))
              false))
      :rule and_simplify)

(step $test_$and_simplify_32
      (@cl (= (and (not (not (not (not a)))) true (not (not (not a))) b)
              false))
      :rule and_simplify)


;-------------
; $or_simplify_rec
;-------------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

; First case of $or_simplify_rec:
; ⊥∨⋯∨⊥ ⇒ ⊥
(step $test_$or_simplify_rec_1 true 
      :rule $assert_eq 
      :args (($or_simplify_rec (or false false)) false))

(step $test_$or_simplify_rec_1 true 
      :rule $assert_eq 
      :args (($or_simplify_rec (or false a b false))
             ($f_list_cons or a b)))

; Second case of $or_simplify_rec:
; 𝜑1 ∨ ⋯ ∨ 𝜑𝑛 ⇒ 𝜑1 ∨ ⋯ ∨ 𝜑𝑛′ where the right-hand side has every repeated 
; literal removed.
(step $test_$or_simplify_rec_2 true 
      :rule $assert_eq 
      :args (($or_simplify_rec (or a a b)) 
             ($f_list_cons or a b)))

(step $test_$or_simplify_rec_3 true 
      :rule $assert_eq 
      :args (($or_simplify_rec (or a a b c c)) 
             ($f_list_cons or a ($f_list_cons or b c))))


; Third case of $or_simplify_rec:
; ⊤ ∨ ⋯ ∨ 𝜑𝑛 ⇒ ⊤
(step $test_$or_simplify_rec_4 true 
      :rule $assert_eq 
      :args (($or_simplify_rec (or a true false)) 
             true))

(step $test_$or_simplify_rec_4b true 
      :rule $assert_eq 
      :args (($or_simplify_rec (or a true b)) 
             true))

(step $test_$or_simplify_rec_4c true 
      :rule $assert_eq 
      :args (($or_simplify_rec (or a a false true b)) 
             true))

; Fourth case of $or_simplify_rec:
; 𝜑𝑖 ∨ ⋯ ∨ 𝜑𝑗 ∨ ⋯ ∨ 𝜑𝑛 ⇒ ⊤ and 𝜑𝑖 , 𝜑𝑗 are such that one is the
; negation of the other formula
(step $test_$or_simplify_rec_5 true 
      :rule $assert_eq 
      :args (($or_simplify_rec (or a (not a)))
             true))

(step $test_$or_simplify_rec_6 true 
      :rule $assert_eq 
      :args (($or_simplify_rec (or a b false b (not a)))
             true))

(step $test_$or_simplify_rec_7 true 
      :rule $assert_eq 
      :args (($or_simplify_rec (or a b false b (not b)))
             true))


;-------------
; or_simplify
;-------------

; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

; First case of or_simplify:
; ⊥∨⋯∨⊥⇒⊥
(step $test_$or_simplify_1
      (@cl (= false false))
      :rule or_simplify)

(step $test_$or_simplify_2
      (@cl (= (or false false) false))
      :rule or_simplify)

(step $test_$or_simplify_3
      (@cl (= (or false false false) false))
      :rule or_simplify)

(step $test_$or_simplify_4
      (@cl (= (or false false false false) false))
      :rule or_simplify)

; Second case of or_simplify
; 𝜑1 ∨⋯∨𝜑𝑛 ⇒ 𝜑1 ∨⋯∨ 𝜑𝑛′ where the right-hand side has all ⊥literals removed.
(step $test_$or_simplify_5
      (@cl (= (or a false) a))
      :rule or_simplify)

(step $test_$or_simplify_6
      (@cl (= (or false a) a))
      :rule or_simplify)

(step $test_$or_simplify_7
      (@cl (= (or false a false) a))
      :rule or_simplify)

(step $test_$or_simplify_8
      (@cl (= (or false false a false) a))
      :rule or_simplify)

(step $test_$or_simplify_9
      (@cl (= (or false false false a) a))
      :rule or_simplify)

(step $test_$or_simplify_10
      (@cl (= (or false a false b) 
              ($f_list_cons or a b)))
      :rule or_simplify)

(step $test_$or_simplify_11
      (@cl (= (or false a b c false) 
              ($f_list_cons or a ($f_list_cons or b c))))
      :rule or_simplify)

; Third case of or_simplify
; 𝜑1 ∨⋯∨𝜑𝑛⇒𝜑1 ∨⋯∨𝜑𝑛′ where the right-hand side has all repeated literals
; removed
(step $test_$or_simplify_12
      (@cl (= (or a a false) a))
      :rule or_simplify)

(step $test_$or_simplify_13
      (@cl (= (or a false a) a))
      :rule or_simplify)

(step $test_$or_simplify_14
      (@cl (= (or a false a false) a))
      :rule or_simplify)

(step $test_$or_simplify_15
      (@cl (= (or false a false a false) a))
      :rule or_simplify)

(step $test_$or_simplify_16
      (@cl (= (or false a a a) a))
      :rule or_simplify)

(step $test_$or_simplify_17
      (@cl (= (or false a b false a b) 
              ($f_list_cons or a b)))
      :rule or_simplify)

(step $test_$or_simplify_18
      (@cl (= (or false a b c c false a) 
              ($f_list_cons or b ($f_list_cons or c a))))
      :rule or_simplify)

; Fourth case of or_simplify
; 𝜑1 ∨⋯∨⊤∨⋯∨𝜑𝑛 ⇒⊤
(step $test_$or_simplify_19
      (@cl (= (or a true) true))
      :rule or_simplify)

(step $test_$or_simplify_20
      (@cl (= (or a true a) true))
      :rule or_simplify)

(step $test_$or_simplify_21
      (@cl (= (or a false a true) true))
      :rule or_simplify)

(step $test_$or_simplify_22
      (@cl (= (or false a true a false) true))
      :rule or_simplify)

(step $test_$or_simplify_23
      (@cl (= (or false a a true) true))
      :rule or_simplify)

(step $test_$or_simplify_24
      (@cl (= (or false a b false true b) 
              true))
      :rule or_simplify)

(step $test_$or_simplify_25
      (@cl (= (or false a b c c true a) 
              true))
      :rule or_simplify)

; Fifth case of or_simplify:
; - 𝜑𝑖 ∨ ⋯ ∨ 𝜑𝑗 ∨ ⋯ ∨ 𝜑𝑛 ⇒ ⊤ and 𝜑𝑖 , 𝜑𝑗 are such that one is the
;   negation of the other formula
(step $test_$or_simplify_26
      (@cl (= (or (not a) a) 
              true))
      :rule or_simplify)

(step $test_$or_simplify_27
      (@cl (= (or a (not a)) 
              true))
      :rule or_simplify)

(step $test_$or_simplify_28
      (@cl (= (or a (not (not (not a))))
              true))
      :rule or_simplify)

(step $test_$or_simplify_29
      (@cl (= (or (not (not a)) (not (not (not a))))
              true))
      :rule or_simplify)

(step $test_$or_simplify_30
      (@cl (= (or (not (not a)) false (not (not (not a))))
              true))
      :rule or_simplify)

(step $test_$or_simplify_31
      (@cl (= (or (not (not (not (not a)))) false (not (not (not a))))
              true))
      :rule or_simplify)

(step $test_$or_simplify_32
      (@cl (= (or (not (not (not (not a)))) false (not (not (not a))) b)
              true))
      :rule or_simplify)

;-------------
; not_simplify
;-------------

; Some constants to build terms, for testing purposes.
(declare-const a Bool)

; Removing leading double negations
(step $test_$not_simplify_1
      (@cl (= (not (not true)) true))
      :rule not_simplify)

(step $test_$not_simplify_2
      (@cl (= (not (not false)) false))
      :rule not_simplify)

(step $test_$not_simplify_3
      (@cl (= (not (not a)) a))
      :rule not_simplify)

(step $test_$not_simplify_4
      (@cl (= (not (not (not (not false)))) false))
      :rule not_simplify)

(step $test_$not_simplify_5
      (@cl (= (not (not (not (not (not a))))) (not a)))
      :rule not_simplify)

; Negation over constants
(step $test_$not_simplify_6
      (@cl (= (not true) false))
      :rule not_simplify)

(step $test_$not_simplify_7
      (@cl (= (not false) true))
      :rule not_simplify)

(step $test_$not_simplify_8
      (@cl (= (not (not (not (not (not false))))) true))
      :rule not_simplify)

(step $test_$not_simplify_9
      (@cl (= (not (not (not (not (not true))))) false))
      :rule not_simplify)


;---------------------
; $check_prod_simplify
;---------------------

; Some constants to build terms, for testing purposes.
(declare-const a Bool)

; Arith terms that can be evaluated
(step $test_$check_prod_simplify_1 true 
      :rule $assert_eq 
      :args (($check_prod_simplify (* 1.0 0.0) 0.0) true))

(step $test_$check_prod_simplify_2 true 
      :rule $assert_eq 
      :args (($check_prod_simplify (* 1.0 0.0) (* 0.0 1.0)) true))

(step $test_$check_prod_simplify_3 true 
      :rule $assert_eq 
      :args (($check_prod_simplify (* 1.0 2.0 3.0) 6.0) true))

(step $test_$check_prod_simplify_4 true 
      :rule $assert_eq 
      :args (($check_prod_simplify (* 1.0 2.0 3.0 4.0) 24.0) true))

; Arith terms that cannot be evaluated
(step $test_$check_prod_simplify_5 true 
      :rule $assert_eq 
      :args (($check_prod_simplify (* 1.0 a) a) true))

(step $test_$check_prod_simplify_6 true 
      :rule $assert_eq 
      :args (($check_prod_simplify (* a 0.0) 0.0) true))

(step $test_$check_prod_simplify_7 true 
      :rule $assert_eq 
      :args (($check_prod_simplify (* 1.0 a 3.0) 
                                   ($f_list_cons * 3.0 a)) true))

;---------------------
; $prod_simplify
;---------------------

; Some constants to build terms, for testing purposes.
(declare-const a Bool)

; Arith terms that can be evaluated
(step $test_$prod_simplify_1 
      (@cl (= (* 1.0 0.0) 0.0)) 
      :rule prod_simplify)

(step $test_$prod_simplify_2 
      (@cl (= (* 1.0 0.0) (* 0.0 1.0))) 
      :rule prod_simplify)

(step $test_$prod_simplify_3 
      (@cl (= (* 1.0 2.0 3.0) 6.0)) 
      :rule prod_simplify)

(step $test_$prod_simplify_4 
      (@cl (= (* 1.0 2.0 3.0 4.0) 24.0)) 
      :rule prod_simplify)

;-------------------------
; $bool_simplify_small_step
;-------------------------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

; First case of $bool_simplify_small_step
(step $test_$bool_simplify_small_step_1 true 
      :rule $assert_eq 
      :args (($bool_simplify_small_step (not (=> a b)))
             ($f_list_cons and a (not b))))

; Second case of $bool_simplify_small_step
(step $test_$bool_simplify_small_step_2 true 
      :rule $assert_eq 
      :args (($bool_simplify_small_step (not ($f_list_cons or a b)))
             ($f_list_cons and (not a) (not b))))

; Third case of $bool_simplify_small_step
(step $test_$bool_simplify_small_step_3 true 
      :rule $assert_eq 
      :args (($bool_simplify_small_step (not ($f_list_cons and a b)))
             ($f_list_cons or (not a) (not b))))

; Fourth case of $bool_simplify_small_step
(step $test_$bool_simplify_small_step_4 true 
      :rule $assert_eq 
      :args (($bool_simplify_small_step (=> a (=> b c)))
             (=> ($f_list_cons and a b) c)))

; Fifth case of $bool_simplify_small_step
(step $test_$bool_simplify_small_step_5 true 
      :rule $assert_eq 
      :args (($bool_simplify_small_step (=> (=> a b) b))
             ($f_list_cons or a b)))

; Sixth case of $bool_simplify_small_step
(step $test_$bool_simplify_small_step_6 true 
      :rule $assert_eq 
      :args (($bool_simplify_small_step ($f_list_cons and a (=> a b)))
             ($f_list_cons and a b)))

; Seventh case of $bool_simplify_small_step
(step $test_$bool_simplify_small_step_7 true 
      :rule $assert_eq 
      :args (($bool_simplify_small_step ($f_list_cons and (=> a b) a))
             ($f_list_cons and a b)))



;-------------------
; $bool_simplify_rec
;-------------------
; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)

; First case of $bool_simplify_rec
(step $test_$bool_simplify_rec_1 true 
      :rule $assert_eq 
      :args (($bool_simplify_rec (not (=> a b)))
             ($f_list_cons and a (not b))))

; Second case of $bool_simplify_rec
(step $test_$bool_simplify_rec_2 true 
      :rule $assert_eq 
      :args (($bool_simplify_rec (not ($f_list_cons or a b)))
             ($f_list_cons and (not a) (not b))))

; Third case of $bool_simplify_rec
(step $test_$bool_simplify_rec_3 true 
      :rule $assert_eq 
      :args (($bool_simplify_rec (not ($f_list_cons and a b)))
             ($f_list_cons or (not a) (not b))))

; Fourth case of $bool_simplify_rec
(step $test_$bool_simplify_rec_4 true 
      :rule $assert_eq 
      :args (($bool_simplify_rec (=> a (=> b c)))
             (=> ($f_list_cons and a b) c)))

; Fifth case of $bool_simplify_rec
(step $test_$bool_simplify_rec_5 true 
      :rule $assert_eq 
      :args (($bool_simplify_rec (=> (=> a b) b))
             ($f_list_cons or a b)))

; Sixth case of $bool_simplify_rec
(step $test_$bool_simplify_rec_6 true 
      :rule $assert_eq 
      :args (($bool_simplify_rec ($f_list_cons and a (=> a b)))
             ($f_list_cons and a b)))

; Seventh case of $bool_simplify_rec
(step $test_$bool_simplify_rec_7 true 
      :rule $assert_eq 
      :args (($bool_simplify_rec ($f_list_cons and (=> a b) a))
             ($f_list_cons and a b)))

;-------------
; bool_simplify
;-------------

; Some constants to build terms, for testing purposes.
(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(declare-const d Bool)
(declare-const e Bool)
(declare-const p (-> Bool Bool))

; First case
(step $test_$bool_simplify_1
      (@cl (= (not (=> a b)) ($f_list_cons and a (not b))))
      :rule bool_simplify)

; Second case
(step $test_$bool_simplify_2
      (@cl (= (not ($f_list_cons or a b)) ($f_list_cons and (not a) (not b))))
      :rule bool_simplify)

; Third case
(step $test_$bool_simplify_3
      (@cl (= (not ($f_list_cons and a b)) ($f_list_cons or (not a) (not b))))
      :rule bool_simplify)

; Fourth case
(step $test_$bool_simplify_4
      (@cl (= (=> a (=> b c)) (=> ($f_list_cons and a b) c)))
      :rule bool_simplify)

; Fifth case
(step $test_$bool_simplify_5
      (@cl (= (=> (=> a b) b) ($f_list_cons or a b)))
      :rule bool_simplify)

; Sixth case
(step $test_$bool_simplify_6
      (@cl (= ($f_list_cons and a (=> a b)) ($f_list_cons and a b)))
      :rule bool_simplify)

; Seventh case
(step $test_$bool_simplify_7
      (@cl (= ($f_list_cons and (=> a b) a) ($f_list_cons and a b)))
      :rule bool_simplify)

; Misc.
(step $test_$bool_simplify_misc_1
      (@cl (= (=> (p a) 
                 (=> ($f_list_cons or (= a b) e) 
                    (p c))) 

              (=> ($f_list_cons and (p a) 
                      ($f_list_cons or (= a b) e)) 
                 (p c))))
      :rule bool_simplify)


;----------------------
; $check_free_variables
;----------------------

(step $test_$check_free_variables_1 true 
      :rule $assert_eq 
      :args (($check_free_variables (@varlist (eo::var "x" Int) (eo::var "y" Int))
                                    (@varlist (eo::var "y" Int))
                                    @varlist.nil)

             true))

(step $test_$check_free_variables_2 true 
      :rule $assert_eq 
      :args (($check_free_variables (@varlist (eo::var "x" Int) (eo::var "y" Int))
                                    (@varlist (eo::var "y" Int))
                                    (@varlist (eo::var "x" Int)))

             false))

(step $test_$check_free_variables_2 true 
      :rule $assert_eq 
      :args (($check_free_variables (@varlist (eo::var "x" Int) 
                                              (eo::var "y" Int)
                                              (eo::var "z" Int))
                                    (@varlist (eo::var "y" Int)
                                              (eo::var "z" Int))
                                    (@varlist (eo::var "y" Int)))

             true))

(step $test_$check_free_variables_2 true 
      :rule $assert_eq 
      :args (($check_free_variables (@varlist (eo::var "x" Int) 
                                              (eo::var "y" Int)
                                              (eo::var "z" Int))
                                    (@varlist (eo::var "y" Int)
                                              (eo::var "z" Int))
                                    (@varlist (eo::var "y" Int)
                                              (eo::var "z" Int)))

             true))

;----------------------
; $check_qnt_rm_unused
;----------------------

(step $test_$check_qnt_rm_unused_1 true 
      :rule $assert_eq 
      :args (($check_qnt_rm_unused 
              (@cl (= (forall (@varlist (eo::var "x" Int) (eo::var "y" Int))
                              (forall (@varlist (eo::var "x" Int)) true))
                      (forall (@varlist (eo::var "y" Int))
                              (forall (@varlist (eo::var "x" Int)) true)))))

             true))

(step $test_$check_qnt_rm_unused_2 true 
      :rule $assert_eq 
      :args (($check_qnt_rm_unused 
              (@cl (= (forall (@varlist (eo::var "x" Int) (eo::var "y" Int)
                                        (eo::var "z" Int))
                              (forall (@varlist (eo::var "x" Int)) true))
                      (forall (@varlist (eo::var "y" Int))
                              (forall (@varlist (eo::var "x" Int)) true)))))

             true))

(step $test_$check_qnt_rm_unused_3 true 
      :rule $assert_eq 
      :args (($check_qnt_rm_unused 
              (@cl (= (exists (@varlist (eo::var "x" Int) (eo::var "y" Int))
                              (exists (@varlist (eo::var "x" Int)) true))
                      (exists (@varlist (eo::var "y" Int))
                              (exists (@varlist (eo::var "x" Int)) true)))))

             true))

(step $test_$check_qnt_rm_unused_4 true 
      :rule $assert_eq 
      :args (($check_qnt_rm_unused 
              (@cl (= (exists (@varlist (eo::var "x" Int) (eo::var "y" Int)
                                        (eo::var "z" Int))
                              (exists (@varlist (eo::var "x" Int)) true))
                      (exists (@varlist (eo::var "y" Int))
                              (exists (@varlist (eo::var "x" Int)) true)))))

             true))

;----------------------
; qnt_rm_unused
;----------------------

(step $test_qnt_rm_unused_1 
      (@cl (= (forall (@varlist (eo::var "x" Int) (eo::var "y" Int))
                              (forall (@varlist (eo::var "x" Int)) true))
                      (forall (@varlist (eo::var "y" Int))
                              (forall (@varlist (eo::var "x" Int)) true)))) 
      :rule qnt_rm_unused)

(step $test_qnt_rm_unused_2 
      (@cl (= (forall (@varlist (eo::var "x" Int) (eo::var "y" Int)
                                        (eo::var "z" Int))
                              (forall (@varlist (eo::var "x" Int)) true))
                      (forall (@varlist (eo::var "y" Int))
                              (forall (@varlist (eo::var "x" Int)) true)))) 
      :rule qnt_rm_unused)

(step $test_qnt_rm_unused_3 
      (@cl (= (exists (@varlist (eo::var "x" Int) (eo::var "y" Int))
                              (exists (@varlist (eo::var "x" Int)) true))
                      (exists (@varlist (eo::var "y" Int))
                              (exists (@varlist (eo::var "x" Int)) true)))) 
      :rule qnt_rm_unused)

(step $test_qnt_rm_unused_4 
      (@cl (= (exists (@varlist (eo::var "x" Int) (eo::var "y" Int)
                                        (eo::var "z" Int))
                              (exists (@varlist (eo::var "x" Int)) true))
                      (exists (@varlist (eo::var "y" Int))
                              (exists (@varlist (eo::var "x" Int)) true))))
      :rule qnt_rm_unused)

;-------------
; eq_symmetric
;-------------

; Some constants to build terms, for testing purposes.
(declare-const a Int)
(declare-const b Int)
(declare-const c Real)
(declare-const d Real)
(declare-const S Type)
(declare-const e S)
(declare-const f S)
(declare-const g (-> S S))

; Arbitrary arith terms
(step $test_$eq_symmetric_1
      (@cl (= (= 1 2) (= 2 1)))
      :rule eq_symmetric)

(step $test_$eq_symmetric_2
      (@cl (= (= 1.0 2.0) (= 2.0 1.0)))
      :rule eq_symmetric)

(step $test_$eq_symmetric_3
      (@cl (= (= a b) (= b a)))
      :rule eq_symmetric)

(step $test_$eq_symmetric_4
      (@cl (= (= (+ a 1) b) (= b (+ a 1))))
      :rule eq_symmetric)

(step $test_$eq_symmetric_5
      (@cl (= (= (+ c 1) d) (= d (+ c 1))))
      :rule eq_symmetric)

; Boolean terms

(step $test_$eq_symmetric_6
      (@cl (= (= true false) (= false true)))
      :rule eq_symmetric)

(step $test_$eq_symmetric_7
      (@cl (= (= (or true true) false) (= false (or true true))))
      :rule eq_symmetric)

(step $test_$eq_symmetric_8
      (@cl (= (= (or true true) (and false true)) 
              (= (and false true) (or true true))))
      :rule eq_symmetric)

; Arbitrary sorts

(step $test_$eq_symmetric_9
      (@cl (= (= e f) (= f e)))
      :rule eq_symmetric)

(step $test_$eq_symmetric_10
      (@cl (= (= e e) (= e e)))
      :rule eq_symmetric)

(step $test_$eq_symmetric_11
      (@cl (= (= (g e) e) (= e (g e))))
      :rule eq_symmetric)

(step $test_$eq_symmetric_12
      (@cl (= (= (g e) (g (g f))) (= (g (g f)) (g e))))
      :rule eq_symmetric)

;-------------
; qnt_simplify
;-------------
; NOTE: it would be interesting to be able to check the cases where this rule should
;       not apply.
(step $test_qnt_simplify_1
      (@cl (= (forall @varlist.nil true) true))
      :rule qnt_simplify)

(step $test_qnt_simplify_2
      (@cl (= (forall @varlist.nil false) false))
      :rule qnt_simplify)

;---------------------------
; eq_simplify_small_step
;---------------------------

(declare-const a Int)
(declare-const c Real)

; Case 𝑡 ≈ 𝑡 ⇒ ⊤
(step $test_$eq_simplify_small_step_1 true
      :rule $assert_eq 
      :args (($eq_simplify_small_step (= 13 13))

             true))

(step $test_$eq_simplify_small_step_2 true
      :rule $assert_eq 
      :args (($eq_simplify_small_step (= 13/2 13/2))

             true))

(step $test_$eq_simplify_small_step_3 true
      :rule $assert_eq 
      :args (($eq_simplify_small_step (= a a))

             true))

(step $test_$eq_simplify_small_step_4 true
      :rule $assert_eq 
      :args (($eq_simplify_small_step (= b b))

             true))

; Case (𝑡1 ≈ 𝑡2) ⇒ ⊥if 𝑡1 and 𝑡2 are different numeric constants
(step $test_$eq_simplify_small_step_5 true
      :rule $assert_eq 
      :args (($eq_simplify_small_step (= 1 2))

             false))

(step $test_$eq_simplify_small_step_6 true
      :rule $assert_eq 
      :args (($eq_simplify_small_step (= 1/1 2/1))

             false))

(step $test_$eq_simplify_small_step_7 true
      :rule $assert_eq 
      :args (($eq_simplify_small_step (= 1 a))

             (= 1 a)))

(step $test_$eq_simplify_small_step_8 true
      :rule $assert_eq 
      :args (($eq_simplify_small_step (= b 1/1))

             (= b 1/1)))

; Case ¬(𝑡 ≈ 𝑡) ⇒ ⊥ if 𝑡 is a numeric constant.
(step $test_$eq_simplify_small_step_9 true
      :rule $assert_eq 
      :args (($eq_simplify_small_step (not (= 1 1)))

             false))

(step $test_$eq_simplify_small_step_10 true
      :rule $assert_eq 
      :args (($eq_simplify_small_step (not (= 1/1 1/1)))

             false))

(step $test_$eq_simplify_small_step_11 true
      :rule $assert_eq 
      :args (($eq_simplify_small_step (not (= 1/1 b)))

             (not (= 1/1 b))))

(step $test_$eq_simplify_small_step_12 true
      :rule $assert_eq 
      :args (($eq_simplify_small_step (not (= a 1)))

             (not (= a 1))))

;---------------------------
; eq_simplify
;---------------------------

(declare-const a Int)
(declare-const b Real)

; Case 𝑡 ≈ 𝑡 ⇒ ⊤
(step $test_$eq_simplify_small_step_1 
      (@cl (= (= 13 13) true))
      :rule eq_simplify)

(step $test_$eq_simplify_small_step_2 
      (@cl (= (= 13/2 13/2) true))
      :rule eq_simplify)

(step $test_$eq_simplify_small_step_3 
      (@cl (= (= a a) true))
      :rule eq_simplify)

(step $test_$eq_simplify_small_step_4 
      (@cl (= (= b b) true))
      :rule eq_simplify)

; Case (𝑡1 ≈ 𝑡2) ⇒ ⊥if 𝑡1 and 𝑡2 are different numeric constants
(step $test_$eq_simplify_small_step_5 
      (@cl (= (= 1 2) false))
      :rule eq_simplify)

(step $test_$eq_simplify_small_step_6
      (@cl (= (= 1/1 2/1) false))
      :rule eq_simplify)

(step $test_$eq_simplify_small_step_7 
      (@cl (= (= 1 a) (= 1 a)))
      :rule eq_simplify)

(step $test_$eq_simplify_small_step_8 
      (@cl (= (= b 1/1) (= b 1/1)))
      :rule eq_simplify)

; Case ¬(𝑡 ≈ 𝑡) ⇒ ⊥ if 𝑡 is a numeric constant.
(step $test_$eq_simplify_small_step_9 
      (@cl (= (not (= 1 1)) false))
      :rule eq_simplify)

(step $test_$eq_simplify_small_step_10 
      (@cl (= (not (= 1/1 1/1)) false))
      :rule eq_simplify)

(step $test_$eq_simplify_small_step_11 
      (@cl (= (not (= 1/1 b)) (not (= 1/1 b))))
      :rule eq_simplify)

(step $test_$eq_simplify_small_step_12 
      (@cl (= (not (= a 1)) (not (= a 1))))
      :rule eq_simplify)

;---------------------------
; div_simplify_small_step
;--------------------------

(declare-const a Int)
(declare-const c Real)

; Case 𝑡 / 𝑡 ⇒ 1
(step $test_$div_simplify_small_step_1 true
      :rule $assert_eq 
      :args (($div_simplify_small_step (/ 13 13))

             1))

(step $test_$div_simplify_small_step_2 true
      :rule $assert_eq 
      :args (($div_simplify_small_step (/ 13/1 13/1))

             1/1))

(step $test_$div_simplify_small_step_3 true
      :rule $assert_eq 
      :args (($div_simplify_small_step (/ a a))

             1))

(step $test_$div_simplify_small_step_4 true
      :rule $assert_eq 
      :args (($div_simplify_small_step (/ c c))

             1/1))

; Case 𝑡 / 1 ⇒ t
(step $test_$div_simplify_small_step_5 true
      :rule $assert_eq 
      :args (($div_simplify_small_step (/ 13 1))

             13))

(step $test_$div_simplify_small_step_6 true
      :rule $assert_eq 
      :args (($div_simplify_small_step (/ 13/1 1/1))

             13/1))

(step $test_$div_simplify_small_step_7 true
      :rule $assert_eq 
      :args (($div_simplify_small_step (/ a 1))

             a))

(step $test_$div_simplify_small_step_8 true
      :rule $assert_eq 
      :args (($div_simplify_small_step (/ c 1/1))

             c))

; Case 𝑡1 / 𝑡2 ⇒𝑡3 if 𝑡1 and 𝑡2 are constants and 𝑡3 is 𝑡1 divided by 𝑡2 according 
; to the semantics of the current theory.
(step $test_$div_simplify_small_step_9 true
      :rule $assert_eq 
      :args (($div_simplify_small_step (/ 13 2))

             6))

(step $test_$div_simplify_small_step_10 true
      :rule $assert_eq 
      :args (($div_simplify_small_step (/ 13/1 2/1))

             13/2))

; Misc. 

(step $test_$div_simplify_small_step_11 true
      :rule $assert_eq 
      :args (($div_simplify_small_step (/ a 19))

             (/ a 19)))

(step $test_$div_simplify_small_step_12 true
      :rule $assert_eq 
      :args (($div_simplify_small_step (/ c 3/4))

             (/ c 3/4)))

;---------------------------
; div_simplify_rec
;---------------------------

(declare-const a Int)
(declare-const c Real)

; Case 𝑡 / 𝑡 ⇒ 1
(step $test_$div_simplify_rec_1 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ 13 13))

             1))

(step $test_$div_simplify_rec_2 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ 13/1 13/1))

             1/1))

(step $test_$div_simplify_rec_3 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ a a))

             1))

(step $test_$div_simplify_rec_4 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ c c))

             1/1))

; Case 𝑡 / 1 ⇒ t
(step $test_$div_simplify_rec_5 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ 13 1))

             13))

(step $test_$div_simplify_rec_6 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ 13/1 1/1))

             13/1))

(step $test_$div_simplify_rec_7 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ a 1))

             a))

(step $test_$div_simplify_rec_8 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ c 1/1))

             c))

; Case 𝑡1 / 𝑡2 ⇒𝑡3 if 𝑡1 and 𝑡2 are constants and 𝑡3 is 𝑡1 divided by 𝑡2 according 
; to the semantics of the current theory.
(step $test_$div_simplify_rec_9 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ 13 2))

             6))

(step $test_$div_simplify_rec_10 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ 13/1 2/1))

             13/2))

; Misc. 
(step $test_$div_simplify_rec_11 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ a 19))

             (/ a 19)))

(step $test_$div_simplify_rec_12 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ c 3/4))

             (/ c 3/4)))

; Testing evaluation order.
(step $test_$div_simplify_rec_13 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ (/ a a) a))

             (/ 1 a)))

(step $test_$div_simplify_rec_14 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ c (/ c c)))

             c))

(step $test_$div_simplify_rec_15 true
      :rule $assert_eq 
      :args (($div_simplify_rec (/ (/ 2/2 2/2) (/ c c)))

             1/1))

;---------------------------
; div_simplify
;---------------------------

(declare-const a Int)
(declare-const c Real)

; Case 𝑡 / 𝑡 ⇒ 1
(step $test_div_simplify_1 
      (@cl (= (/ 13 13) 1))
      :rule div_simplify)

(step $test_div_simplify_2 
      (@cl (= (/ 13/1 13/1) 1/1))
      :rule div_simplify)

(step $test_div_simplify_3 
      (@cl (= (/ a a) 1))
      :rule div_simplify)

(step $test_div_simplify_4 
      (@cl (= (/ c c) 1/1))
      :rule div_simplify)

; Case 𝑡 / 1 ⇒ t
(step $test_div_simplify_5 
      (@cl (= (/ 13 1) 13))
      :rule div_simplify)

(step $test_div_simplify_6 
      (@cl (= (/ 13/1 1/1) 13/1))
      :rule div_simplify)

(step $test_div_simplify_7 
      (@cl (= (/ a 1) a))
      :rule div_simplify)

(step $test_div_simplify_8 
      (@cl (= (/ c 1/1) c))
      :rule div_simplify)

; Case 𝑡1 / 𝑡2 ⇒𝑡3 if 𝑡1 and 𝑡2 are constants and 𝑡3 is 𝑡1 divided by 𝑡2 according 
; to the semantics of the current theory.
(step $test_div_simplify_9 
      (@cl (= (/ 13 2) 6))
      :rule div_simplify)

(step $test_div_simplify_10 
      (@cl (= (/ 13/1 2/1) 13/2))
      :rule div_simplify)

; Misc. 
(step $test_div_simplify_11 
      (@cl (= (/ a 19) (/ a 19)))
      :rule div_simplify)

(step $test_div_simplify_12 
      (@cl (= (/ c 3/4) (/ c 3/4)))
      :rule div_simplify)

; Testing evaluation order.
(step $test_div_simplify_13 
      (@cl (= (/ (/ a a) a) (/ 1 a)))
      :rule div_simplify)

(step $test_div_simplify_14 
      (@cl (= (/ c (/ c c)) c))
      :rule div_simplify)

(step $test_div_simplify_15 
      (@cl (= (/ (/ 2/2 2/2) (/ c c)) 1/1))
      :rule div_simplify)

;---------------------------
; comp_simplify_small_step
;---------------------------

(declare-const a Int)
(declare-const b Int)

; Case 𝑠1 < 𝑠2 ⇒ ⊤, if 𝑠1, 𝑠2 are numerical constants and 𝑠1 is strictly less than 𝑠2
(step $test_$comp_simplify_small_step_1 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (< 1 2))

             true))

(step $test_$comp_simplify_small_step_2 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (< 1.0 2.0))

             true))

; Case 𝑠1 < 𝑠2 ⇒ ⊥, if 𝑠1, 𝑠2 are numerical constants and 𝑠1 is greater or equal than 𝑠2
(step $test_$comp_simplify_small_step_3 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (< 2 1))

             false))

(step $test_$comp_simplify_small_step_4 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (< 2.0 1.0))

             false))

; Case 𝑠1 ≤ 𝑠2 ⇒ ⊤ if 𝑠1, 𝑠2 are numerical constants and 𝑠1 is less or equal than 𝑠2.
(step $test_$comp_simplify_small_step_5 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (<= 1 2))

             true))

(step $test_$comp_simplify_small_step_6 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (<= 2.0 2.0))

             true))

; Case 𝑠1 ≤ 𝑠2 ⇒ ⊥ if 𝑠1, 𝑠2 are numerical constants and 𝑠1 is strictly greater than 𝑠2
(step $test_$comp_simplify_small_step_5 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (<= 2 1))

             false))

(step $test_$comp_simplify_small_step_6 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (<= 4.0 2.0))

             false))

; Case 𝑠 < 𝑠 ⇒ ⊥
(step $test_$comp_simplify_small_step_6 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (< 2 2))

             false))

(step $test_$comp_simplify_small_step_7 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (< 4.0 4.0))

             false))

(step $test_$comp_simplify_small_step_8 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (< a a))

             false))

; Case 𝑠 <= 𝑠 ⇒ ⊤
(step $test_$comp_simplify_small_step_9 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (<= 2 2))

             true))

(step $test_$comp_simplify_small_step_10 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (<= 4.0 4.0))

             true))

(step $test_$comp_simplify_small_step_11 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (<= a a))

             true))

; Case 𝑠1 ≥ 𝑠2 ⇒ 𝑠2 ≤ 𝑠1
(step $test_$comp_simplify_small_step_12 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (>= a b))

             (<= b a)))

(step $test_$comp_simplify_small_step_13 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (>= 1 2))

             (<= 2 1)))

; Case 𝑠1 < 𝑠2 ⇒ not (𝑠2 ≤ 𝑠1)
(step $test_$comp_simplify_small_step_14 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (< a b))

             (not (<= b a))))

; Case 𝑠1 > 𝑠2 ⇒ not (𝑠1 ≤ 𝑠2)
(step $test_$comp_simplify_small_step_14 true
      :rule $assert_eq 
      :args (($comp_simplify_small_step (> a b))

             (not (<= a b))))

;---------------------------
; $comp_simplify_rec
;---------------------------

(declare-const a Int)
(declare-const b Int)

; Case 𝑠1 < 𝑠2 ⇒ ⊤, if 𝑠1, 𝑠2 are numerical constants and 𝑠1 is strictly less than 𝑠2
(step $test_$comp_simplify_rec_1 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (< 1 2))

             true))

(step $test_$comp_simplify_rec_2 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (< 1.0 2.0))

             true))

; Case 𝑠1 < 𝑠2 ⇒ ⊥, if 𝑠1, 𝑠2 are numerical constants and 𝑠1 is greater or equal than 𝑠2
(step $test_$comp_simplify_rec_3 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (< 2 1))

             false))

(step $test_$comp_simplify_rec_4 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (< 2.0 1.0))

             false))

; Case 𝑠1 ≤ 𝑠2 ⇒ ⊤ if 𝑠1, 𝑠2 are numerical constants and 𝑠1 is less or equal than 𝑠2.
(step $test_$comp_simplify_rec_5 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (<= 1 2))

             true))

(step $test_$comp_simplify_rec_6 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (<= 2.0 2.0))

             true))

; Case 𝑠1 ≤ 𝑠2 ⇒ ⊥ if 𝑠1, 𝑠2 are numerical constants and 𝑠1 is strictly greater than 𝑠2
(step $test_$comp_simplify_rec_5 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (<= 2 1))

             false))

(step $test_$comp_simplify_rec_6 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (<= 4.0 2.0))

             false))

; Case 𝑠 < 𝑠 ⇒ ⊥
(step $test_$comp_simplify_rec_6 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (< 2 2))

             false))

(step $test_$comp_simplify_rec_7 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (< 4.0 4.0))

             false))

(step $test_$comp_simplify_rec_8 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (< a a))

             false))

; Case 𝑠 <= 𝑠 ⇒ ⊤
(step $test_$comp_simplify_rec_9 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (<= 2 2))

             true))

(step $test_$comp_simplify_rec_10 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (<= 4.0 4.0))

             true))

(step $test_$comp_simplify_rec_11 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (<= a a))

             true))

; Case 𝑠1 ≥ 𝑠2 ⇒ 𝑠2 ≤ 𝑠1
(step $test_$comp_simplify_rec_12 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (>= a b))

             (<= b a)))

(step $test_$comp_simplify_rec_13 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (>= 1 2))

             false))

; Case 𝑠1 < 𝑠2 ⇒ not (𝑠2 ≤ 𝑠1)
(step $test_$comp_simplify_rec_14 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (< a b))

             (not (<= b a))))

; Case 𝑠1 > 𝑠2 ⇒ not (𝑠1 ≤ 𝑠2)
(step $test_$comp_simplify_rec_14 true
      :rule $assert_eq 
      :args (($comp_simplify_rec (> a b))

             (not (<= a b))))


;---------------------------
; comp_simplify
;---------------------------

(declare-const a Int)
(declare-const b Int)

; Case 𝑠1 < 𝑠2 ⇒ ⊤, if 𝑠1, 𝑠2 are numerical constants and 𝑠1 is strictly less than 𝑠2
(step $test_comp_simplify_1 
      (@cl (= (< 1 2) true))
      :rule comp_simplify)

(step $test_comp_simplify_2 
      (@cl (= (< 1.0 2.0) true))
      :rule comp_simplify)

; Case 𝑠1 < 𝑠2 ⇒ ⊥, if 𝑠1, 𝑠2 are numerical constants and 𝑠1 is greater or equal than 𝑠2
(step $test_comp_simplify_3 
      (@cl (= (< 2 1) false))
      :rule comp_simplify)

(step $test_comp_simplify_4 
      (@cl (= (< 2.0 1.0) false))
      :rule comp_simplify)

; Case 𝑠1 ≤ 𝑠2 ⇒ ⊤ if 𝑠1, 𝑠2 are numerical constants and 𝑠1 is less or equal than 𝑠2.
(step $test_comp_simplify_5 
      (@cl (= (<= 1 2) true))
      :rule comp_simplify)

(step $test_comp_simplify_6 
      (@cl (= (<= 2.0 2.0) true))
      :rule comp_simplify)

; Case 𝑠1 ≤ 𝑠2 ⇒ ⊥ if 𝑠1, 𝑠2 are numerical constants and 𝑠1 is strictly greater than 𝑠2
(step $test_comp_simplify_5 
      (@cl (= (<= 2 1) false))
      :rule comp_simplify)

(step $test_comp_simplify_6 
      (@cl (= (<= 4.0 2.0) false))
      :rule comp_simplify)

; Case 𝑠 < 𝑠 ⇒ ⊥
(step $test_comp_simplify_6 
      (@cl (= (< 2 2) false))
      :rule comp_simplify)

(step $test_comp_simplify_7 
      (@cl (= (< 4.0 4.0) false))
      :rule comp_simplify)

(step $test_comp_simplify_8 
      (@cl (= (< a a) false))
      :rule comp_simplify)

; Case 𝑠 <= 𝑠 ⇒ ⊤
(step $test_comp_simplify_9 
      (@cl (= (<= 2 2) true))
      :rule comp_simplify)

(step $test_comp_simplify_10 
      (@cl (= (<= 4.0 4.0) true))
      :rule comp_simplify)

(step $test_comp_simplify_11 
      (@cl (= (<= a a) true))
      :rule comp_simplify)

; Case 𝑠1 ≥ 𝑠2 ⇒ 𝑠2 ≤ 𝑠1
(step $test_comp_simplify_12 
      (@cl (= (>= a b) (<= b a)))
      :rule comp_simplify)

(step $test_comp_simplify_13 
      (@cl (= (>= 1 2) false))
      :rule comp_simplify)

; Case 𝑠1 < 𝑠2 ⇒ not (𝑠2 ≤ 𝑠1)
(step $test_comp_simplify_14 
      (@cl (= (< a b) (not (<= b a))))
      :rule comp_simplify)

; Case 𝑠1 > 𝑠2 ⇒ not (𝑠1 ≤ 𝑠2)
(step $test_comp_simplify_14 
      (@cl (= (> a b) (not (<= a b))))
      :rule comp_simplify)

;---------------------------
; $ite_simplify_small_step
;---------------------------

(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(declare-const d Bool)

; Case (ite ⊤ 𝑡1 𝑡2) ⇒ 𝑡1
(step $test_$ite_simplify_small_step_1 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite true a b))

             a))

(step $test_$ite_simplify_small_step_2 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite true (or a b) b))

             (or a b)))

; Case (ite ⊥ 𝑡1 𝑡2) ⇒ 𝑡2
(step $test_$ite_simplify_small_step_3 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite false a b))

             b))

(step $test_$ite_simplify_small_step_4 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite false a (or a b)))

             (or a b)))

; Case (ite 𝜓 𝑡 𝑡) ⇒ 𝑡
(step $test_$ite_simplify_small_step_5 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite a b b))

             b))

(step $test_$ite_simplify_small_step_6 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite a (or a b) (or a b)))

             (or a b)))

; Case (ite ¬𝜑 𝑡1 𝑡2) ⇒ (ite 𝜑 𝑡2 𝑡1)
(step $test_$ite_simplify_small_step_7 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite (not a) b c))

             (ite a c b)))

(step $test_$ite_simplify_small_step_8 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite (not (or a a)) b c))

             (ite (or a a) c b)))

; Case (ite 𝜓 (ite 𝜓 𝑡1 𝑡2) 𝑡3) ⇒ (ite 𝜓 𝑡1 𝑡3)
(step $test_$ite_simplify_small_step_9 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite a (ite a b c) d))

             (ite a b d)))

(step $test_$ite_simplify_small_step_10 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite (or a a) (ite (or a a) b c) d))

             (ite (or a a) b d)))

; Case (ite 𝜓 𝑡1 (ite 𝜓 𝑡2 𝑡3) ⇒ (ite 𝜓 𝑡1 𝑡3)
(step $test_$ite_simplify_small_step_11 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite a b (ite a c d)))

             (ite a b d)))

(step $test_$ite_simplify_small_step_12 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite (or a a) b (ite (or a a) c d)))

             (ite (or a a) b d)))

; Case (ite 𝜓 ⊤ ⊥) ⇒ 𝜓
(step $test_$ite_simplify_small_step_13 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite a true false))

             a))

(step $test_$ite_simplify_small_step_14 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite (and a b c) true false))

             (and a b c)))

; Case (ite 𝜓 ⊥ ⊤) ⇒ ¬ 𝜓
(step $test_$ite_simplify_small_step_15 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite a false true))

             (not a)))

(step $test_$ite_simplify_small_step_16 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite (and a b c) false true))

             (not (and a b c))))

; Case (ite 𝜓 ⊤ 𝜑) ⇒ 𝜓 ∨ 𝜑
(step $test_$ite_simplify_small_step_17 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite a true b))

             (or a b)))

(step $test_$ite_simplify_small_step_18 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite (or a b) true (and b c)))

             (or (or a b) (and b c))))

; Case (ite  𝜓 𝜑 ⊥) ⇒ 𝜓 ∧ 𝜑
(step $test_$ite_simplify_small_step_19 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite a b false))

             (and a b)))

(step $test_$ite_simplify_small_step_20 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite (or a b) (and b c) false))

             (and (or a b) (and b c))))

; Case (ite 𝜓 ⊥ 𝜑) ⇒ not 𝜓 ∧ 𝜑
(step $test_$ite_simplify_small_step_21 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite a false b))

             (and (not a) b)))

(step $test_$ite_simplify_small_step_22 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite (or a b) false (and c d)))

             (and (not (or a b)) (and c d))))

; Case (ite 𝜓 𝜑 ⊤) ⇒ ¬𝜓 ∨ 𝜑
(step $test_$ite_simplify_small_step_22 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite a b true))

             (or (not a) b)))

(step $test_$ite_simplify_small_step_23 true
      :rule $assert_eq 
      :args (($ite_simplify_small_step (ite (or a b) (and c d) true))

             (or (not (or a b)) (and c d))))


;---------------------------
; $ite_simplify_rec
;---------------------------

(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(declare-const d Bool)

; Case (ite ⊤ 𝑡1 𝑡2) ⇒ 𝑡1
(step $test_$ite_simplify_rec_1 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite true a b))

             a))

(step $test_$ite_simplify_rec_2 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite true (or a b) b))

             (or a b)))

; Case (ite ⊥ 𝑡1 𝑡2) ⇒ 𝑡2
(step $test_$ite_simplify_rec_3 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite false a b))

             b))

(step $test_$ite_simplify_rec_4 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite false a (or a b)))

             (or a b)))

; Case (ite 𝜓 𝑡 𝑡) ⇒ 𝑡
(step $test_$ite_simplify_rec_5 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite a b b))

             b))

(step $test_$ite_simplify_rec_6 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite a (or a b) (or a b)))

             (or a b)))

; Case (ite ¬𝜑 𝑡1 𝑡2) ⇒ (ite 𝜑 𝑡2 𝑡1)
(step $test_$ite_simplify_rec_7 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite (not a) b c))

             (ite a c b)))

(step $test_$ite_simplify_rec_8 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite (not (or a a)) b c))

             (ite (or a a) c b)))

; Case (ite 𝜓 (ite 𝜓 𝑡1 𝑡2) 𝑡3) ⇒ (ite 𝜓 𝑡1 𝑡3)
(step $test_$ite_simplify_rec_9 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite a (ite a b c) d))

             (ite a b d)))

(step $test_$ite_simplify_rec_10 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite (or a a) (ite (or a a) b c) d))

             (ite (or a a) b d)))

; Case (ite 𝜓 𝑡1 (ite 𝜓 𝑡2 𝑡3) ⇒ (ite 𝜓 𝑡1 𝑡3)
(step $test_$ite_simplify_rec_11 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite a b (ite a c d)))

             (ite a b d)))

(step $test_$ite_simplify_rec_12 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite (or a a) b (ite (or a a) c d)))

             (ite (or a a) b d)))

; Case (ite 𝜓 ⊤ ⊥) ⇒ 𝜓
(step $test_$ite_simplify_rec_13 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite a true false))

             a))

(step $test_$ite_simplify_rec_14 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite (and a b c) true false))

             (and a b c)))

; Case (ite 𝜓 ⊥ ⊤) ⇒ ¬ 𝜓
(step $test_$ite_simplify_rec_15 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite a false true))

             (not a)))

(step $test_$ite_simplify_rec_16 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite (and a b c) false true))

             (not (and a b c))))

; Case (ite 𝜓 ⊤ 𝜑) ⇒ 𝜓 ∨ 𝜑
(step $test_$ite_simplify_rec_17 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite a true b))

             (or a b)))

(step $test_$ite_simplify_rec_18 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite (or a b) true (and b c)))

             (or (or a b) (and b c))))

; Case (ite  𝜓 𝜑 ⊥) ⇒ 𝜓 ∧ 𝜑
(step $test_$ite_simplify_rec_19 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite a b false))

             (and a b)))

(step $test_$ite_simplify_rec_20 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite (or a b) (and b c) false))

             (and (or a b) (and b c))))

; Case (ite 𝜓 ⊥ 𝜑) ⇒ not 𝜓 ∧ 𝜑
(step $test_$ite_simplify_rec_21 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite a false b))

             (and (not a) b)))

(step $test_$ite_simplify_rec_22 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite (or a b) false (and c d)))

             (and (not (or a b)) (and c d))))

; Case (ite 𝜓 𝜑 ⊤) ⇒ ¬𝜓 ∨ 𝜑
(step $test_$ite_simplify_rec_22 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite a b true))

             (or (not a) b)))

(step $test_$ite_simplify_rec_23 true
      :rule $assert_eq 
      :args (($ite_simplify_rec (ite (or a b) (and c d) true))

             (or (not (or a b)) (and c d))))

;---------------------------
; ite_simplify
;---------------------------

(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(declare-const d Bool)

; Case (ite ⊤ 𝑡1 𝑡2) ⇒ 𝑡1
(step $test_ite_simplify_1 
      (@cl (= (ite true a b) a))
      :rule ite_simplify)

(step $test_ite_simplify_2 
      (@cl (= (ite true (or a b) b) (or a b)))
      :rule ite_simplify)

; Case (ite ⊥ 𝑡1 𝑡2) ⇒ 𝑡2
(step $test_ite_simplify_3 
      (@cl (= (ite false a b) b))
      :rule ite_simplify)

(step $test_ite_simplify_4 
      (@cl (= (ite false a (or a b)) (or a b)))
      :rule ite_simplify)

; Case (ite 𝜓 𝑡 𝑡) ⇒ 𝑡
(step $test_ite_simplify_5 
      (@cl (= (ite a b b) b))
      :rule ite_simplify)

(step $test_ite_simplify_6 
      (@cl (= (ite a (or a b) (or a b)) (or a b)))
      :rule ite_simplify)

; Case (ite ¬𝜑 𝑡1 𝑡2) ⇒ (ite 𝜑 𝑡2 𝑡1)
(step $test_ite_simplify_7 
      (@cl (= (ite (not a) b c) (ite a c b)))
      :rule ite_simplify)

(step $test_ite_simplify_8 
      (@cl (= (ite (not (or a a)) b c) (ite (or a a) c b)))
      :rule ite_simplify)

; Case (ite 𝜓 (ite 𝜓 𝑡1 𝑡2) 𝑡3) ⇒ (ite 𝜓 𝑡1 𝑡3)
(step $test_ite_simplify_9 
      (@cl (= (ite a (ite a b c) d) (ite a b d)))
      :rule ite_simplify)

(step $test_ite_simplify_10 
      (@cl (= (ite (or a a) (ite (or a a) b c) d) (ite (or a a) b d)))
      :rule ite_simplify)

; Case (ite 𝜓 𝑡1 (ite 𝜓 𝑡2 𝑡3) ⇒ (ite 𝜓 𝑡1 𝑡3)
(step $test_ite_simplify_11 
      (@cl (= (ite a b (ite a c d)) (ite a b d)))
      :rule ite_simplify)

(step $test_ite_simplify_12 
      (@cl (= (ite (or a a) b (ite (or a a) c d)) (ite (or a a) b d)))
      :rule ite_simplify)

; Case (ite 𝜓 ⊤ ⊥) ⇒ 𝜓
(step $test_ite_simplify_13 
      (@cl (= (ite a true false) a))
      :rule ite_simplify)

(step $test_ite_simplify_14 
      (@cl (= (ite (and a b c) true false) (and a b c)))
      :rule ite_simplify)

; Case (ite 𝜓 ⊥ ⊤) ⇒ ¬ 𝜓
(step $test_ite_simplify_15 
      (@cl (= (ite a false true) (not a)))
      :rule ite_simplify)

(step $test_ite_simplify_16 
      (@cl (= (ite (and a b c) false true) (not (and a b c))))
      :rule ite_simplify)

; Case (ite 𝜓 ⊤ 𝜑) ⇒ 𝜓 ∨ 𝜑
(step $test_ite_simplify_17 
      (@cl (= (ite a true b) (or a b)))
      :rule ite_simplify)

(step $test_ite_simplify_18 
      (@cl (= (ite (or a b) true (and b c)) (or (or a b) (and b c))))
      :rule ite_simplify)

; Case (ite  𝜓 𝜑 ⊥) ⇒ 𝜓 ∧ 𝜑
(step $test_ite_simplify_19 
      (@cl (= (ite a b false) (and a b)))
      :rule ite_simplify)

(step $test_ite_simplify_20 
      (@cl (= (ite (or a b) (and b c) false) (and (or a b) (and b c))))
      :rule ite_simplify)

; Case (ite 𝜓 ⊥ 𝜑) ⇒ not 𝜓 ∧ 𝜑
(step $test_ite_simplify_21 
      (@cl (= (ite a false b) (and (not a) b)))
      :rule ite_simplify)

(step $test_ite_simplify_22 
      (@cl (= (ite (or a b) false (and c d)) (and (not (or a b)) (and c d))))
      :rule ite_simplify)

; Case (ite 𝜓 𝜑 ⊤) ⇒ ¬𝜓 ∨ 𝜑
(step $test_ite_simplify_22 
      (@cl (= (ite a b true) (or (not a) b)))
      :rule ite_simplify)

(step $test_ite_simplify_23 
      (@cl (= (ite (or a b) (and c d) true) (or (not (or a b)) (and c d))))
      :rule ite_simplify)


;---------------------------
; $minus_simplify_small_step
;---------------------------

(declare-const a Int)
(declare-const b Int)
(declare-const c Real)
(declare-const d Real)

; Case t - t => 0
(step $test_$minus_simplify_small_step_1 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- a a))

             0))

(step $test_$minus_simplify_small_step_2 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- c c))

             0/1))

(step $test_$minus_simplify_small_step_3 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- 3 3))

             0))

(step $test_$minus_simplify_small_step_4 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- 3/1 3/1))

             0/1))

; Case 𝑡1 − 𝑡2 ⇒ 𝑡3, where 𝑡1 and 𝑡2 are numerical constants and 𝑡3 is 𝑡2 subtracted 
(step $test_$minus_simplify_small_step_5 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- 3 2))

             1))

(step $test_$minus_simplify_small_step_6 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- 3/1 2/1))

             1/1))

(step $test_$minus_simplify_small_step_7 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- 3/2 3/4))

             3/4))

; Case t - 0 => t
(step $test_$minus_simplify_small_step_8 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- a 0))

             a))

(step $test_$minus_simplify_small_step_9 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- c 0))

             c))

(step $test_$minus_simplify_small_step_10 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- 10 0))

             10))

(step $test_$minus_simplify_small_step_11 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- 10/1 0/1))

             10/1))

; Case 0 - t => - t
(step $test_$minus_simplify_small_step_12 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- 0 a))

             (- a)))

(step $test_$minus_simplify_small_step_13 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- 0 c))

             (- c)))

(step $test_$minus_simplify_small_step_14 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- 0 10))

             (- 10)))

(step $test_$minus_simplify_small_step_15 true
      :rule $assert_eq 
      :args (($minus_simplify_small_step (- 0/1 10/1))

             (- 10/1)))


;---------------------------
; $minus_simplify_rec
;---------------------------

(declare-const a Int)
(declare-const b Int)
(declare-const c Real)
(declare-const d Real)

; Case t - t => 0
(step $test_$minus_simplify_rec_1 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- a a))

             0))

(step $test_$minus_simplify_rec_2 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- c c))

             0/1))

(step $test_$minus_simplify_rec_3 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- 3 3))

             0))

(step $test_$minus_simplify_rec_4 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- 3/1 3/1))

             0/1))

; Case 𝑡1 − 𝑡2 ⇒ 𝑡3, where 𝑡1 and 𝑡2 are numerical constants and 𝑡3 is 𝑡2 subtracted 
(step $test_$minus_simplify_rec_5 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- 3 2))

             1))

(step $test_$minus_simplify_rec_6 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- 3/1 2/1))

             1/1))

(step $test_$minus_simplify_rec_7 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- 3/2 3/4))

             3/4))

; Case t - 0 => t
(step $test_$minus_simplify_rec_8 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- a 0))

             a))

(step $test_$minus_simplify_rec_9 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- c 0))

             c))

(step $test_$minus_simplify_rec_10 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- 10 0))

             10))

(step $test_$minus_simplify_rec_11 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- 10/1 0/1))

             10/1))

; Case 0 - t => - t
(step $test_$minus_simplify_rec_12 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- 0 a))

             (- a)))

(step $test_$minus_simplify_rec_13 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- 0 c))

             (- c)))

(step $test_$minus_simplify_rec_14 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- 0 10))

             (- 10)))

(step $test_$minus_simplify_rec_15 true
      :rule $assert_eq 
      :args (($minus_simplify_rec (- 0/1 10/1))

             (- 10/1)))

;---------------------------
; minus_simplify
;---------------------------

(declare-const a Int)
(declare-const b Int)
(declare-const c Real)
(declare-const d Real)

; Case t - t => 0
(step $test_minus_simplify_1 
      (@cl (= (- a a) 0))
      :rule minus_simplify)

(step $test_minus_simplify_2 
      (@cl (= (- c c) 0/1))
      :rule minus_simplify)

(step $test_minus_simplify_3 
      (@cl (= (- 3 3) 0))
      :rule minus_simplify)

(step $test_minus_simplify_4 
      (@cl (= (- 3/1 3/1) 0/1))
      :rule minus_simplify)

; Case 𝑡1 − 𝑡2 ⇒ 𝑡3, where 𝑡1 and 𝑡2 are numerical constants and 𝑡3 is 𝑡2 subtracted 
(step $test_minus_simplify_5 
      (@cl (= (- 3 2) 1))
      :rule minus_simplify)

(step $test_minus_simplify_6 
      (@cl (= (- 3/1 2/1) 1/1))
      :rule minus_simplify)

(step $test_minus_simplify_7 
      (@cl (= (- 3/2 3/4) 3/4))
      :rule minus_simplify)

; Case t - 0 => t
(step $test_minus_simplify_8 
      (@cl (= (- a 0) a))
      :rule minus_simplify)

(step $test_minus_simplify_9 
      (@cl (= (- c 0) c))
      :rule minus_simplify)

(step $test_minus_simplify_10 
      (@cl (= (- 10 0) 10))
      :rule minus_simplify)

(step $test_minus_simplify_11 
      (@cl (= (- 10/1 0/1) 10/1))
      :rule minus_simplify)

; Case 0 - t => - t
(step $test_minus_simplify_12 
      (@cl (= (- 0 a) (- a)))
      :rule minus_simplify)

(step $test_minus_simplify_13 
      (@cl (= (- 0 c) (- c)))
      :rule minus_simplify)

(step $test_minus_simplify_14 
      (@cl (= (- 0 10) (- 10)))
      :rule minus_simplify)

(step $test_minus_simplify_15 
      (@cl (= (- 0/1 10/1) (- 10/1)))
      :rule minus_simplify)

;---------------------------
; unary_minus_simplify
;---------------------------

(declare-const a Int)
(declare-const c Real)

; Case (- (- t)) => t
(step $test_unary_minus_simplify_1 
      (@cl (= (- (- a)) a))
      :rule unary_minus_simplify)

(step $test_unary_minus_simplify_2
      (@cl (= (- (- 1)) 1))
      :rule unary_minus_simplify)

(step $test_unary_minus_simplify_3
      (@cl (= (- (- 3/2)) 3/2))
      :rule unary_minus_simplify)

(step $test_unary_minus_simplify_4
      (@cl (= (- (- (+ a b))) (+ a b)))
      :rule unary_minus_simplify)

; Case (- t) => u, for numerical constant t, u, such that u is negative t.
(step $test_unary_minus_simplify_5
      (@cl (= (- 1) -1))
      :rule unary_minus_simplify)

(step $test_unary_minus_simplify_6
      (@cl (= (- 2/3) -2/3))
      :rule unary_minus_simplify)

(step $test_unary_minus_simplify_7
      (@cl (= (- 0/1) -0/1))
      :rule unary_minus_simplify)

(step $test_unary_minus_simplify_8
      (@cl (= (- 0) -0))
      :rule unary_minus_simplify)
